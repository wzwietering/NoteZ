from setuptools import setup

setup(name='NoteZ',
      description='A text file editor with natural language processing',
      author='Wilmer Zwietering',
      version='0.1',
      requires=['nltk', 'gensim'])
